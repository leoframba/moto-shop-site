{
	"name": "moto-shop-repo",
	"private": true,
	"packageManager": "yarn@4.12.0",
	"workspaces": [
		"apps/*"
	],
	"devDependencies": {
		"husky": "^8.0.0",
		"lint-staged": "^15.2.2"
	},
	"scripts": {
		"prepare": "husky install",
		"lint": "yarn workspace frontend lint && yarn workspace backend lint",
		"format": "yarn workspace frontend format && yarn workspace backend format",
		"check-all": "yarn lint && yarn format",
		"test": "yarn workspace frontend test && yarn workspace backend test",
		"test:integration": "yarn workspace frontend test:integration",
		"test:e2e": "yarn workspace frontend test:e2e"
	},
	"dependencies": {
		"sonner": "^2.0.7"
	}
}
